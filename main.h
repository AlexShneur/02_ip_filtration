#pragma once
#include <iostream>
#include <vector>
#include <array>

using namespace std;

constexpr int N = 4;                        //bytes count in the ip-address
using my_ip_array = vector<array<int, N>>;